name: focus_on_words_app
description: Focus on Words
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4
  url_launcher: ^6.0.18
  cupertino_icons: ^1.0.2
  flutter_svg: ^1.0.0
  url_strategy: ^0.2.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
